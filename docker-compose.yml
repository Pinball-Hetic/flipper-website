# Production : build multi-étapes (standalone) — `docker compose up --build`
services:
  next-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flipper-website
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: always
