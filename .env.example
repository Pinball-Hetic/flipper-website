DATABASE_URL=postgresql://user:password@localhost:5432/flipper
DIRECT_URL=postgresql://user:password@localhost:5432/flipper

BORNE_API_KEY=change-me-in-production

# Contrat /v1 (bornes) — auth Bearer. Fallback dev sur BORNE_API_KEY si absent.
CABINET_KEY=change-me-in-production
# Base URL utilisée pour construire le claimUrl renvoyé par POST /v1/scores
CLAIM_BASE_URL=http://localhost:8888
